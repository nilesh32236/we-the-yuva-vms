# Fix Summary — Audit Issue #44

Applied fixes for 28 audit findings (12 important, 16 minor):

## Important Fixes

1. **`frontend/lib/api.ts:67`** — Moved stale Authorization header overwrite inside else/catch block so refreshed token isn't replaced by stale one on expired tokens.

2. **`frontend/components/org/OrgProfileForm.tsx:38`** — Replaced manual useState + validate() with react-hook-form's useForm + zodResolver(RegisterOrganizationSchema), matching BlogPostForm pattern. Fixes query key to plural ['organizations', org.id] and mutationFn type.

3. **`frontend/components/opportunities/OpportunityForm.tsx:307`** — Changed query key from `['opportunities', 'locations']` to `['locations']` so location cache is not invalidated by opportunity mutations.

4. **`frontend/components/opportunities/OpportunityCard.tsx:79`** — Added `exact: true` on getQueriesData/setQueriesData to prevent matching `['opportunities', 'locations']` cache.

5. **`frontend/components/events/AttendanceChecklist.tsx:47`** — Changed useEffect to use functional updater that only adds new volunteers, preserving user's unsaved toggle changes.

6. **`frontend/components/events/AttendanceChecklist.tsx:78`** — Added error toast in handleSave catch block instead of silently swallowing errors.

7. **`frontend/components/events/AttendanceChecklist.tsx:91`** — Added error toast in handleApprove catch block instead of silently swallowing errors.

8. **`frontend/components/events/EventForm.tsx:49`** — Added wrappedOnSubmit that catches errors and maps them via setError('root') to display server errors above the submit button.

9. **`frontend/components/admin/UserTable.tsx:57`** — Replaced raw api.patch with manual loading state with useMutation from @tanstack/react-query, including automatic cache invalidation and consistent error handling.

10. **`frontend/components/levels/ProofUploadForm.tsx:27`** — Removed explicit Content-Type: multipart/form-data header so axios auto-detects the correct boundary.

11. **`frontend/components/shared/FileUpload.tsx:47`** — Already fixed (no explicit Content-Type header).

## Minor Fixes

12. **`frontend/components/opportunities/OpportunityForm.tsx:341`** — Added Zod schema (CreateLocationSchema) for client-side location creation validation before POST.

13. **`frontend/components/opportunities/OpportunityCard.tsx:7`** — Replaced relative imports with @/ path alias.

14. **`frontend/components/layout/TopNav.tsx:107,115`** — Already had onError handlers.

15. **`frontend/components/admin/UserTable.tsx:63`** — Already used error.normalizedMessage.

16. **`frontend/components/admin/UserTable.tsx:234`** — Backend accepts role name string (looks up UUID internally), no change needed.

17. **`frontend/components/events/EventForm.tsx:75`** — Removed setValueAs timezone conversion for datetime-local; passes raw local-time string to server.

18. **`frontend/components/shared/Pagination.tsx:8`** — Changed setPage type from `((prev: number) => number) => void` to `(page: number) => void` with direct value calls.

19. **`frontend/components/levels/ProofUploadForm.tsx:20`** — Already had error message for oversized files.

20. **`frontend/components/levels/ProofUploadForm.tsx:93`** — Added MIME type validation (image/*, application/pdf) in handleFile for drag-and-drop enforcement.

21. **`frontend/components/shared/FileUpload.tsx:52`** — Added server error extraction from response.data.error before falling back to normalizedMessage.

22. **`frontend/components/landing/FeaturedOpportunities.tsx:17`** — Added console.error before returning empty array.

23. **`frontend/components/landing/BlogPreview.tsx:19`** — Added console.error before returning empty array.

24. **`frontend/components/auth/ResendButton.tsx:31`** — Already had catch block.

## Not Changed
- **EventCard.tsx:27** — _count normalization considered out of scope (minor, would need API normalization layer)
