# PWA Offline Audit Fix Summary

**Issue:** #43 — 0 critical, 2 important, 1 minor in `frontend/hooks/useOfflineCheckin.ts`

## Changes

### Important — Queue not cleared on logout (line 20)
- Imported `useAuth` from `@/lib/auth-context`
- Added `useEffect` watching `user` transitions from logged-in to logged-out
- Calls `clearQueue()` and resets `queuedCount` to 0 on logout
- Prevents offline check-ins from a previous session being submitted under a new user

### Important — No retry mechanism on sync failure (line 23)
- After `syncQueuedCheckins()`, if `result.failed > 0`, schedules a retry with exponential backoff (10s, 30s, 60s)
- Uses `useRef` for timeout tracking, cleans up on unmount
- Exposed `retrySync` function in return value for manual UI retry buttons

### Minor — Partial sync fires both callbacks (line 24)
- Combined success/error callbacks into a single invocation:
  - `result.failed === 0` → `onSuccess()` 
  - `result.failed > 0` → `onError('Synced X of Y check-in(s)')`

## Files Modified
- `frontend/hooks/useOfflineCheckin.ts`

## Verification
- `pnpm typecheck` — ✅ 0 errors
- `pnpm lint` — ✅ 0 errors
